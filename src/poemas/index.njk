---
layout: base.njk
title: Poemas
permalink: /poemas/
---

<h1>Poemas</h1>

<p>Quando as palavras precisam de ritmo, silêncio e espaço para respirar.</p>

<ul class="post-list">
  {% for post in collections.poemas | reverse %}
  <li>
    <a href="{{ post.url }}">{{ post.data.title }}</a>
    <span class="post-meta">
      <span class="reading-time">{{ post.templateContent | tempoLeitura }}</span>
      <span class="date">{{ post.date | dataPT }}</span>
    </span>
  </li>
  {% endfor %}
</ul>

{% if collections.poemas.length == 0 %}
<p><em>Em breve, novos poemas serão publicados aqui.</em></p>
{% endif %}
