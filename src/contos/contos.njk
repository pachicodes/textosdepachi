---
layout: base.njk
title: Contos
---

<h1>Contos</h1>

<p>Histórias curtas para ler em uma sentada — ou para guardar e revisitar.</p>

<ul class="post-list">
  {% for post in collections.contos | reverse %}
  <li>
    <a href="{{ post.url }}">{{ post.data.title }}</a>
    <span class="date">{{ post.date | dataPT }}</span>
    {% if post.data.excerpt %}
    <p class="excerpt">{{ post.data.excerpt }}</p>
    {% endif %}
  </li>
  {% endfor %}
</ul>

{% if collections.contos.length == 0 %}
<p><em>Em breve, novos contos serão publicados aqui.</em></p>
{% endif %}
